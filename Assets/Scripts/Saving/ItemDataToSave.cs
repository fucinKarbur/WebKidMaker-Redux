using System;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace WKMR
{
    [Serializable]
    public class ItemDataToSave
    {
        /* public string ScriptedName;
        public float[] Color;
        public float[] Offset;
        public string Path;

        public ItemDataToSave(ItemTemplate template)
        {
            if (template == null)
            {
                Debug.Log("template is null");
                return;
            }
            ScriptedName = template.Item.name;
            Path = template.Item.Path;

            Color color = template.Image.color;
            Vector3 offset = template.Item.Offset;

            Color = new[] { color.r, color.g, color.b, color.a };
            Offset = new[] { offset.x, offset.y, offset.z };
        } */
    }
}